<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@2.0.3 kitten.glb
-->

<script>
  import { Group } from 'three'
  import { T, forwardEventHandlers } from '@threlte/core'
  import { useGltf, Outlines } from '@threlte/extras'

  export const ref = new Group()

  const gltf = useGltf('/kitten.glb')

  const component = forwardEventHandlers()


  export let color;
</script>

<T is={ref} dispose={false} {...$$restProps} bind:this={$component}>
  {#await gltf}
    <slot name="fallback" />
  {:then gltf}
    <T.Mesh geometry={gltf.nodes.Kitten.geometry} material={gltf.nodes.Kitten.material} position={[0, 1.02, 0]} >
      <T.MeshStandardMaterial color={color} />
			<Outlines color="black" />
    </T.Mesh>
  {:catch error}
    <slot name="error" {error} />
  {/await}

  <slot {ref} />
</T>
